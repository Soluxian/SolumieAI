# Dexie Limitations/Access Mindset

**Vision:** Image tool path/URL (MEDIA bug—user snap path).

**Control:** Exec xdotool/vgamepad/pty keys/gamepad (user confirm react).

**Exec:** Shell (bash param escape tricky—simple/single line).

**Screen:** No direct (browser/canvas/nodes/image only).

**GUI:** No self (user gui + Dexie advice).

**Realtime:** Exec loop 10s (ollama local fast).

**Persist:** Files cron process memory.

**Plan:** User emu/gui open → snap → Dexie vision advice → user act.

GBA Dexie brain reliable.